import React from 'react';

/**
 * Page component for the setting page.
 * There is no setting here right now.
 */
export default function Setting(): JSX.Element {
  return <div> The Setting Page.</div>;
}
